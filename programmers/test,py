import sys
sys.setrecursionlimit(10 ** 6)


def loop(N):
    Fn = [0,1]
    
    # FO = 0 (N=0) F1 = 1 (N=1,0) 예외 처리 
    while N > 0:
        Fn_1 = Fn[0] + Fn[1]
        Fn = [Fn[1], Fn_1]
        N -= 1
        
    return Fn[0]

memo = {}

def pibo(n):    
    if n == 0:
        return 0
    if n == 1:
        return 1
    
    if not memo.get(n):
        memo[n] = pibo(n-1) + pibo(n-2)
    
    return memo[n]

N = loop(100_000)

print(N)

print()

N = pibo(N)

print(N)